#ifndef MAPA2_H
#define MAPA2_H

#include "mapa.h"  // Inclui a definição de MapData

// Funções específicas para o mapa 2
void InitMap2(MapData *map);
void UpdateMap2(MapData *map, Player *player);
void DrawMap2(MapData *map, Player *player);

#endif
